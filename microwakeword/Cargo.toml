[package]
name = "microwakeword"
version = "0.2.0"
edition = "2021"
description = "Lightweight wake word detection using Vosk"
license = "GPL-3.0-only"

[dependencies]
vosk = "0.3.1"
log = "0.4"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
