# Cookie Voice Assistant 🍪

Cookie - это оффлайн голосовой ассистент с персоной JARVIS, разработанный на Rust. Работает полностью локально на вашем компьютере с возможностью использования онлайн API (Gemini) при необходимости.

Проект состоит из двух основных компонентов:
- **Core Engine** (`/app`) - консольное приложение на Rust с полным функционалом ассистента
- **GUI Application** (`/gui`) - графический интерфейс на Svelte + Tauri

## 🌟 Особенности

- **Оффлайн wake-word detection**: RustPotter - активируется по фразам "Cookie" и "Куки"
- **Speech-to-Text (STT)**: Vosk для оффлайн распознавания или Gemini API для онлайн
- **Персона JARVIS**: Формальный, спокойный, уверенный тон общения - "Да, сэр"
- **Голосовые команды**: Настраиваемая система команд через JSON
- **Расширяемость**: Легко добавлять новые команды и интеграции
- **Чистый Rust**: Без C++ toolchain, полностью на Rust

## 🔔 Wake-word через RustPotter

RustPotter включен по умолчанию (feature `rustpotter-wakeword`) и обрабатывает аудио в реальном времени:

1. **Модель** – укажите путь к `.rpw` файлу в `config.json` (`wake_word_path`). В комплекте есть заглушка `assets/wakeword/cookie.rpw`. Для реального использования положите сюда обученную модель (`jarvis-default.rpw`, собственный вариант и т.д.).
2. **Сборка** – `cargo build --release` (feature включен по умолчанию). При необходимости отключите детектор: `cargo build --release --no-default-features`.
3. **Порог срабатывания** – регулируется полем `wake_word_threshold` (значения 0–1). Чем ниже значение, тем чувствительнее активация.
4. **Фразы** – сейчас детектор реагирует на "Cookie" и "Куки". Для кастомных фраз обучите модель через [RustPotter CLI](https://github.com/GiviMAD/rustpotter) и положите файл в `assets/wakeword/`.

При запуске ассистент получает непрерывный поток PCM i16 (16 кГц, моно) и передает его в RustPotter. При срабатывании wake-word система моментально (<10 мс) переключается в режим распознавания команд.

## 🏗️ Архитектура

Проект состоит из двух основных частей:

### Core Engine (`/app`)
Консольное приложение на чистом Rust, включающее:
- **Аудиопайплайн**: CPAL для кроссплатформенной записи микрофона
- **Wake-word detection**: RustPotter (встроенный по умолчанию) с поддержкой Porcupine и Vosk
- **Speech-to-Text**: Gemini API (онлайн) или Vosk (оффлайн, экспериментально)
- **Система команд**: YAML/JSON конфигурации с поддержкой:
  - Запуска процессов и CLI-команд
  - Воспроизведения звуковых файлов
  - AutoHotkey-скриптов (Windows)
  - Цепочек команд (chaining)
  - Условий завершения
- **JARVIS персона**: JSON-файл с фразами для кастомизации тона общения
- **Конфигурация**: JSON-файлы в системных директориях пользователя
- **Логирование**: env_logger для отладки

### GUI Application (`/gui`)
Desktop-приложение на базе Tauri + Svelte:
- **Frontend**: Svelte 3 + Routify (роутинг) + SvelteUI (компоненты)
- **Backend**: Tauri (Rust) с зеркальной копией логики из `/app`:
  - Tauri Commands для взаимодействия с backend
  - События в реальном времени (статус ассистента, логи)
  - Управление конфигурацией через UI
  - Выбор микрофона, STT/wake-word движков
  - Просмотр и управление командами
  - Настройки API ключей
- **Интеграция**: Полная связь между Svelte-компонентами и Rust-backend через IPC
- **Дизайн**: Темная серая тема без эмодзи, минималистичный UI

Оба компонента могут работать независимо друг от друга.

## 🔧 Требования

### Обязательные зависимости

- **Git** 2.30+ — управление версиями
- **Rust** 1.70 или новее + Cargo (устанавливаются через rustup)
- **Node.js** 18+ и **npm** — для фронтенда и Tauri
- **Tauri CLI** 1.0+ (`cargo install tauri-cli`) для локальной сборки GUI

### Системные библиотеки

**Windows:**
- Все зависимости ставятся установщиком Tauri автоматически
- Для разработки желательно установить [Microsoft C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)

**Linux (Debian/Ubuntu):**
```bash
sudo apt install -y libasound2-dev pkg-config libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev
```

**macOS:**
```bash
brew install pkg-config
brew install webkit2gtk gtksourceview5
```

Актуальный список требований для всех платформ: [tauri.app/v1/guides/prerequisites](https://tauri.app/v1/guides/prerequisites)

## 📦 Установка

### 1. Скачайте проект

#### Вариант A: Клонируйте репозиторий с помощью Git

```bash
git clone https://github.com/<your-username>/<repository-name>.git
cd cookie-assistant
```

#### Вариант B: Скачайте ZIP-архив

1. Перейдите на страницу репозитория на GitHub
2. Нажмите зеленую кнопку "Code" → "Download ZIP"
3. Распакуйте архив в удобную папку
4. Откройте терминал в распакованной папке

### 2. Установите зависимости

#### Инструменты для ядра (`/app`)
- Установите Rust и Cargo:
  ```bash
  curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
  source "$HOME/.cargo/env"
  ```
- (Опционально) Обновите toolchain до последней стабильной версии:
  ```bash
  rustup update stable
  ```

#### Инструменты для GUI (`/gui`)
- Установите Node.js версии 18 и выше (или используйте [nvm](https://github.com/nvm-sh/nvm))
- Установите менеджер пакетов npm (идет в комплекте с Node.js)
- Установите Tauri CLI (для запуска и сборки desktop-приложения):
  ```bash
  cargo install tauri-cli
  ```
- На Linux дополнительно установите системные пакеты:
  ```bash
  sudo apt install -y libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev
  ```
  (см. [официальные требования Tauri](https://tauri.app/v1/guides/prerequisites) для macOS/Windows)

### 3. Подготовьте ассеты

#### Wake-word модель (RustPotter)

По умолчанию в проекте есть заглушка модели в `app/assets/wakeword/cookie.rpw`. Для реальной работы:

1. Используйте существующие модели из папки `app/rustpotter/` (jarvis-default.rpw и т.д.)
2. Или создайте свою модель с помощью [RustPotter CLI](https://github.com/GiviMAD/rustpotter)

```bash
# Пример: скопируйте существующую модель
cp app/rustpotter/jarvis-default.rpw app/assets/wakeword/cookie.rpw
```

#### STT модель (Vosk) — опционально

Если хотите использовать оффлайн STT через Vosk:

1. Скачайте русскую модель: https://alphacephei.com/vosk/models
2. Рекомендуется: `vosk-model-small-ru-0.22` (~45 MB)
3. Распакуйте в `app/vosk/model_small`

```bash
# Пример
cd app/vosk
wget https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip
unzip vosk-model-small-ru-0.22.zip
mv vosk-model-small-ru-0.22 model_small
```

**Примечание**: В текущей сборке Vosk отключен по умолчанию. Для онлайн распознавания используйте Gemini API.

### 4. Соберите проект

#### Сборка Core Engine
```bash
cd app
cargo build --release
```

#### Установка зависимостей GUI
```bash
cd gui
npm install
```

## 🚀 Запуск

### Core Engine
```bash
cd app
cargo run --release
```

### GUI (Tauri)
```bash
cd gui
npm run tauri dev
```

При первом запуске создастся конфигурация в:
- **Windows**: `%APPDATA%\cookie\assistant\config.json`
- **Linux**: `~/.config/cookie/assistant/config.json`  
- **macOS**: `~/Library/Application Support/com.cookie.assistant/config.json`

## ⚙️ Конфигурация

Файл `config.json`:

```json
{
  "wake_word_threshold": 0.45,
  "wake_word_path": "assets/wakeword/cookie.rpw",
  "stt_backend": {
    "type": "Gemini"
  },
  "gemini_api_key": "YOUR_API_KEY_HERE",
  "jarvis_phrases": "assets/phrases/jarvis_style.json",
  "commands_path": "commands/commands.json",
  "listening_device": 0
}
```

### Параметры

- `wake_word_threshold` (0.0-1.0): Чувствительность детекции wake-word. Ниже = чувствительнее
- `wake_word_path`: Путь к модели RustPotter (.rpw файл)
- `stt_backend`: 
  - `{"type": "Vosk", "model_path": "vosk/model_small"}` - оффлайн (требует libvosk)
  - `{"type": "Gemini"}` - онлайн через Google Gemini API
- `gemini_api_key`: API ключ для Gemini (получить на https://makersuite.google.com/app/apikey)
- `listening_device`: Индекс микрофона (0 = по умолчанию)

## 🎤 Использование

1. **Запустите приложение** - появится сообщение "🎤 Запуск прослушивания..."
2. **Скажите wake-word**: "Cookie" или "Куки"
3. **Дождитесь ответа**: "Я вас слушаю, сэр."
4. **Произнесите команду** в течение 15 секунд
5. **Получите результат**: Ассистент выполнит команду и ответит

### Примеры команд

- "Привет" → "Здравствуйте, сэр. Чем могу помочь?"
- "Который час" → Покажет текущее время
- "Какое число" → Покажет текущую дату
- "Спасибо" → "До свидания, сэр. Всегда рад помочь."

## 📝 Добавление команд

Откройте `app/commands/commands.json`:

```json
{
  "commands": [
    {
      "id": "my_command",
      "name": "Моя команда",
      "description": "Описание команды",
      "icon": null,
      "keywords": ["ключевое слово", "фраза"],
      "action": {
        "type": "run_process",
        "command": "notepad.exe",
        "args": [],
        "working_dir": null
      }
    }
  ]
}
```

### Типы действий

**1. Запуск процесса** (`run_process`):
```json
{
  "type": "run_process",
  "command": "cmd",
  "args": ["/C", "echo Hello"],
  "working_dir": null
}
```

**2. Воспроизведение аудио** (`play_audio`):
```json
{
  "type": "play_audio",
  "file": "sounds/notification.wav"
}
```

**3. Текстовый ответ** (`respond_text`):
```json
{
  "type": "respond_text",
  "text": "Готово, сэр!"
}
```

## 🎭 Настройка персоны JARVIS

Файл `app/assets/phrases/jarvis_style.json`:

```json
{
  "ack": ["Да, сэр.", "Слушаю, сэр."],
  "processing": ["Работаю над этим, сэр."],
  "done": ["Задача выполнена, сэр."],
  "error": ["Произошла ошибка, сэр."],
  "wake": ["Я вас слушаю, сэр."]
}
```

Добавляйте свои фразы для более разнообразных ответов!

## 🏗️ Структура проекта

```
app/
├── src/
│   ├── main.rs              # Главный модуль
│   ├── config.rs            # Система конфигурации
│   ├── jarvis.rs            # JARVIS персона
│   ├── commands.rs          # Система команд
│   ├── audio_pipeline.rs    # CPAL аудио-пайплайн
│   ├── wakeword/
│   │   ├── mod.rs
│   │   └── rustpotter.rs    # RustPotter детектор
│   └── stt/
│       ├── mod.rs
│       ├── vosk.rs          # Vosk STT (заглушка)
│       └── gemini_audio.rs  # Gemini STT
├── assets/
│   ├── wakeword/
│   │   └── cookie.rpw       # Модель wake-word
│   └── phrases/
│       └── jarvis_style.json
├── commands/
│   └── commands.json        # Библиотека команд
├── rustpotter/              # Дополнительные wake-word модели
└── vosk/                    # Vosk модели (опционально)
```

## 🔍 Отладка

Включите подробное логирование:

**Windows:**
```cmd
set RUST_LOG=debug
cargo run
```

**Linux/macOS:**
```bash
RUST_LOG=debug cargo run
```

## 🛠️ Разработка

```bash
# Проверка кода
cargo check

# Сборка в debug
cargo build

# Сборка в release
cargo build --release

# Тесты
cargo test

# Очистка
cargo clean
```

### Сборка с RustPotter

```bash
cargo build --release --features rustpotter-wakeword
```

### Сборка без RustPotter (только заглушка)

```bash
cargo build --release --no-default-features
```

## ⚠️ Известные ограничения

- **Vosk**: Требует установки libvosk в систему, в текущей сборке отключен
- **RustPotter**: Требуется обученная модель для wake-word
- **TTS**: Синтез речи пока не реализован (ассистент выводит текст в консоль)
- **GUI**: Графический интерфейс в разработке

## 🗺️ Планы развития

- [ ] Интеграция Vosk для оффлайн STT
- [ ] TTS через Piper или Coqui
- [ ] Полноценный GUI через Tauri
- [ ] Расширенная библиотека команд (800+)
- [ ] Детекция тишины для автозавершения команды
- [ ] Поддержка контекстных диалогов
- [ ] Интеграция с AutoHotkey скриптами
- [ ] Поддержка плагинов

## 📄 Лицензия

GPL-3.0-only

## 🤝 Вклад

Приветствуются pull requests! Для серьезных изменений сначала откройте issue.

## 💬 Поддержка

- Issues: https://github.com/your-repo/issues
- Discussions: https://github.com/your-repo/discussions

---

**Примечание**: Проект находится в активной разработке. Некоторые функции могут измениться в будущих версиях.

## 🙏 Благодарности

- [RustPotter](https://github.com/GiviMAD/rustpotter) - за отличный wake-word детектор
- [Vosk](https://alphacephei.com/vosk/) - за оффлайн STT
- [CPAL](https://github.com/RustAudio/cpal) - за кроссплатформенный аудио
- Сообщество Rust - за потрясающую экосистему

Создано с ❤️ на Rust
