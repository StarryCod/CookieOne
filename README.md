# Cookie Voice Assistant 🍪

Cookie - это оффлайн голосовой ассистент с персоной JARVIS, разработанный на Rust. Работает полностью локально на вашем компьютере с возможностью использования онлайн API (Gemini) при необходимости.

## 🌟 Особенности

- **Оффлайн wake-word detection**: RustPotter - активируется по фразам "Cookie" и "Куки"
- **Speech-to-Text (STT)**: Vosk для оффлайн распознавания или Gemini API для онлайн
- **Персона JARVIS**: Формальный, спокойный, уверенный тон общения - "Да, сэр"
- **Голосовые команды**: Настраиваемая система команд через JSON
- **Расширяемость**: Легко добавлять новые команды и интеграции
- **Чистый Rust**: Без C++ toolchain, полностью на Rust

## 🏗️ Архитектура

Проект состоит из двух основных частей:
- `/app` - Core Engine на Rust (аудиопайплайн, wake-word, STT, команды)
- `/gui` - Графический интерфейс на Svelte + Tauri

## 🔧 Требования

### Обязательные зависимости

- **Rust** 1.70 или новее
- **Cargo** (установится с Rust)

### Системные библиотеки

**На Windows:**
- Никаких дополнительных требований

**На Linux:**
```bash
sudo apt-get install libasound2-dev pkg-config
```

**На macOS:**
```bash
brew install pkg-config
```

## 📦 Установка

### 1. Клонируйте репозиторий

```bash
git clone <url-репозитория>
cd cookie-assistant
```

### 2. Подготовка ассетов

#### Wake-word модель (RustPotter)

По умолчанию в проекте есть заглушка модели в `app/assets/wakeword/cookie.rpw`. Для реальной работы:

1. Используйте существующие модели из папки `app/rustpotter/` (jarvis-default.rpw и т.д.)
2. Или создайте свою модель с помощью [RustPotter CLI](https://github.com/GiviMAD/rustpotter)

```bash
# Пример: скопируйте существующую модель
cp app/rustpotter/jarvis-default.rpw app/assets/wakeword/cookie.rpw
```

#### STT модель (Vosk) - опционально

Если хотите использовать оффлайн STT через Vosk:

1. Скачайте русскую модель: https://alphacephei.com/vosk/models
2. Рекомендуется: `vosk-model-small-ru-0.22` (~45 MB)
3. Распакуйте в `app/vosk/model_small`

```bash
# Пример
cd app/vosk
wget https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip
unzip vosk-model-small-ru-0.22.zip
mv vosk-model-small-ru-0.22 model_small
```

**Примечание**: В текущей сборке Vosk отключен по умолчанию. Для онлайн распознавания используйте Gemini API.

### 3. Соберите проект

```bash
cd app
cargo build --release
```

## 🚀 Запуск

```bash
cd app
cargo run --release
```

При первом запуске создастся конфигурация в:
- **Windows**: `%APPDATA%\cookie\assistant\config.json`
- **Linux**: `~/.config/cookie/assistant/config.json`  
- **macOS**: `~/Library/Application Support/com.cookie.assistant/config.json`

## ⚙️ Конфигурация

Файл `config.json`:

```json
{
  "wake_word_threshold": 0.45,
  "wake_word_path": "assets/wakeword/cookie.rpw",
  "stt_backend": {
    "type": "Gemini"
  },
  "gemini_api_key": "YOUR_API_KEY_HERE",
  "jarvis_phrases": "assets/phrases/jarvis_style.json",
  "commands_path": "commands/commands.json",
  "listening_device": 0
}
```

### Параметры

- `wake_word_threshold` (0.0-1.0): Чувствительность детекции wake-word. Ниже = чувствительнее
- `wake_word_path`: Путь к модели RustPotter (.rpw файл)
- `stt_backend`: 
  - `{"type": "Vosk", "model_path": "vosk/model_small"}` - оффлайн (требует libvosk)
  - `{"type": "Gemini"}` - онлайн через Google Gemini API
- `gemini_api_key`: API ключ для Gemini (получить на https://makersuite.google.com/app/apikey)
- `listening_device`: Индекс микрофона (0 = по умолчанию)

## 🎤 Использование

1. **Запустите приложение** - появится сообщение "🎤 Запуск прослушивания..."
2. **Скажите wake-word**: "Cookie" или "Куки"
3. **Дождитесь ответа**: "Я вас слушаю, сэр."
4. **Произнесите команду** в течение 15 секунд
5. **Получите результат**: Ассистент выполнит команду и ответит

### Примеры команд

- "Привет" → "Здравствуйте, сэр. Чем могу помочь?"
- "Который час" → Покажет текущее время
- "Какое число" → Покажет текущую дату
- "Спасибо" → "До свидания, сэр. Всегда рад помочь."

## 📝 Добавление команд

Откройте `app/commands/commands.json`:

```json
{
  "commands": [
    {
      "id": "my_command",
      "name": "Моя команда",
      "description": "Описание команды",
      "icon": null,
      "keywords": ["ключевое слово", "фраза"],
      "action": {
        "type": "run_process",
        "command": "notepad.exe",
        "args": [],
        "working_dir": null
      }
    }
  ]
}
```

### Типы действий

**1. Запуск процесса** (`run_process`):
```json
{
  "type": "run_process",
  "command": "cmd",
  "args": ["/C", "echo Hello"],
  "working_dir": null
}
```

**2. Воспроизведение аудио** (`play_audio`):
```json
{
  "type": "play_audio",
  "file": "sounds/notification.wav"
}
```

**3. Текстовый ответ** (`respond_text`):
```json
{
  "type": "respond_text",
  "text": "Готово, сэр!"
}
```

## 🎭 Настройка персоны JARVIS

Файл `app/assets/phrases/jarvis_style.json`:

```json
{
  "ack": ["Да, сэр.", "Слушаю, сэр."],
  "processing": ["Работаю над этим, сэр."],
  "done": ["Задача выполнена, сэр."],
  "error": ["Произошла ошибка, сэр."],
  "wake": ["Я вас слушаю, сэр."]
}
```

Добавляйте свои фразы для более разнообразных ответов!

## 🏗️ Структура проекта

```
app/
├── src/
│   ├── main.rs              # Главный модуль
│   ├── config.rs            # Система конфигурации
│   ├── jarvis.rs            # JARVIS персона
│   ├── commands.rs          # Система команд
│   ├── audio_pipeline.rs    # CPAL аудио-пайплайн
│   ├── wakeword/
│   │   ├── mod.rs
│   │   └── rustpotter.rs    # RustPotter детектор
│   └── stt/
│       ├── mod.rs
│       ├── vosk.rs          # Vosk STT (заглушка)
│       └── gemini_audio.rs  # Gemini STT
├── assets/
│   ├── wakeword/
│   │   └── cookie.rpw       # Модель wake-word
│   └── phrases/
│       └── jarvis_style.json
├── commands/
│   └── commands.json        # Библиотека команд
├── rustpotter/              # Дополнительные wake-word модели
└── vosk/                    # Vosk модели (опционально)
```

## 🔍 Отладка

Включите подробное логирование:

**Windows:**
```cmd
set RUST_LOG=debug
cargo run
```

**Linux/macOS:**
```bash
RUST_LOG=debug cargo run
```

## 🛠️ Разработка

```bash
# Проверка кода
cargo check

# Сборка в debug
cargo build

# Сборка в release
cargo build --release

# Тесты
cargo test

# Очистка
cargo clean
```

### Сборка с RustPotter

```bash
cargo build --release --features rustpotter-wakeword
```

### Сборка без RustPotter (только заглушка)

```bash
cargo build --release --no-default-features
```

## ⚠️ Известные ограничения

- **Vosk**: Требует установки libvosk в систему, в текущей сборке отключен
- **RustPotter**: Требуется обученная модель для wake-word
- **TTS**: Синтез речи пока не реализован (ассистент выводит текст в консоль)
- **GUI**: Графический интерфейс в разработке

## 🗺️ Планы развития

- [ ] Интеграция Vosk для оффлайн STT
- [ ] TTS через Piper или Coqui
- [ ] Полноценный GUI через Tauri
- [ ] Расширенная библиотека команд (800+)
- [ ] Детекция тишины для автозавершения команды
- [ ] Поддержка контекстных диалогов
- [ ] Интеграция с AutoHotkey скриптами
- [ ] Поддержка плагинов

## 📄 Лицензия

GPL-3.0-only

## 🤝 Вклад

Приветствуются pull requests! Для серьезных изменений сначала откройте issue.

## 💬 Поддержка

- Issues: https://github.com/your-repo/issues
- Discussions: https://github.com/your-repo/discussions

---

**Примечание**: Проект находится в активной разработке. Некоторые функции могут измениться в будущих версиях.

## 🙏 Благодарности

- [RustPotter](https://github.com/GiviMAD/rustpotter) - за отличный wake-word детектор
- [Vosk](https://alphacephei.com/vosk/) - за оффлайн STT
- [CPAL](https://github.com/RustAudio/cpal) - за кроссплатформенный аудио
- Сообщество Rust - за потрясающую экосистему

Создано с ❤️ на Rust
