# ⚡ Быстрый старт Cookie Voice Assistant

## 📋 Минимальные требования

- **Rust** 1.70+ ([установка](https://rustup.rs/))
- **Linux**: `libasound2-dev` + `pkg-config`
- **Windows**: без дополнительных зависимостей
- **macOS**: `pkg-config` через Homebrew

## 🚀 Запуск за 3 шага

### 1. Установите Rust
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
```

### 2. Установите системные зависимости (только Linux)
```bash
sudo apt-get install -y libasound2-dev pkg-config
```

### 3. Соберите и запустите
```bash
cd app
cargo run --release
```

При первом запуске создастся конфигурация в `~/.config/cookie/assistant/config.json`

## ⚙️ Быстрая настройка

### Установите Gemini API ключ
Откройте `~/.config/cookie/assistant/config.json`:
```json
{
  "gemini_api_key": "ВАШ_API_КЛЮЧ_ЗДЕСЬ"
}
```

Получите ключ: https://makersuite.google.com/app/apikey

### Обучите wake-word модель (опционально)
```bash
# 1. Установите RustPotter CLI
cargo install rustpotter-cli

# 2. Запишите образцы
rustpotter-cli record -o cookie_samples/
# Скажите "Cookie" 20-30 раз

# 3. Обучите модель
rustpotter-cli train -i cookie_samples/ -o app/assets/wakeword/cookie.rpw
```

## 🎤 Использование

1. Запустите ассистента: `cargo run --release`
2. Скажите wake-word: **"Cookie"** или **"Куки"**
3. Дождитесь ответа: *"Я вас слушаю, сэр."*
4. Произнесите команду в течение 15 секунд

## 📝 Примеры команд

- **"который час"** → показывает время
- **"какое число"** → показывает дату
- **"открой блокнот"** → открывает Notepad
- **"запусти chrome"** → открывает Google Chrome
- **"привет"** → приветствие
- **"спасибо"** → прощание

**Всего доступно: 811 команд!**

Полный список: `app/commands/commands.json`

## 🔧 Основные настройки

### config.json
```json
{
  "wake_word_threshold": 0.45,      // чувствительность (0.0-1.0)
  "wake_word_path": "assets/wakeword/cookie.rpw",
  "stt_backend": {
    "type": "Gemini"                // или "Vosk" для offline
  },
  "gemini_api_key": "YOUR_KEY",
  "commands_path": "commands/commands.json",
  "listening_device": 0             // индекс микрофона
}
```

## 📚 Дополнительная документация

- **Полная инструкция**: [README.md](README.md)
- **Библиотека команд**: [app/commands/README.md](app/commands/README.md)
- **Отчет о релизе**: [RELEASE_REPORT.md](RELEASE_REPORT.md)

## 🐛 Решение проблем

### Ошибка: "ALSA lib not found"
```bash
sudo apt-get install -y libasound2-dev
```

### Ошибка: "Gemini API error"
Проверьте API ключ в `config.json`

### Wake-word не срабатывает
1. Убедитесь, что модель `cookie.rpw` обучена
2. Понизьте `wake_word_threshold` в конфиге (например, до 0.3)
3. Говорите четко и достаточно громко

### Команды не распознаются
1. Проверьте качество микрофона
2. Убедитесь, что Gemini API настроен
3. Включите debug логи: `RUST_LOG=debug cargo run`

## 💻 Команды для разработки

```bash
# Проверка кода
cargo check

# Сборка release
cargo build --release

# Запуск с логами
RUST_LOG=debug cargo run

# Очистка
cargo clean

# Тесты
cargo test
```

## 🎯 Что дальше?

1. ✅ Настройте Gemini API ключ
2. ✅ Обучите wake-word модель
3. ✅ Добавьте свои команды в `commands/commands.json`
4. ✅ Настройте персону JARVIS в `assets/phrases/jarvis_style.json`
5. ✅ Запустите GUI (требует дополнительной настройки)

## 📞 Поддержка

- **Документация**: См. [README.md](README.md)
- **Команды**: См. [app/commands/README.md](app/commands/README.md)
- **Проблемы**: Запустите с `RUST_LOG=debug` и проверьте логи

---

**Cookie Voice Assistant** — оффлайн голосовой ассистент на Rust  
Версия: 0.1.0 | Лицензия: GPL-3.0
